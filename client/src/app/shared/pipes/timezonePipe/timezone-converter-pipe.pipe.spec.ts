import { TimezoneConverterPipe } from './timezone-converter-pipe.pipe';

describe('TimezoneConverterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimezoneConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
